The database indices for Mascot are stored here.

This prevents the fasta folder from getting cluttered by the Mascot index files.

The .fasta file itself is linked to this directory to prevent copying.
